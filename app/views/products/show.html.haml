= provide :title, @product.name
%h1= @product.name
.product
  .image
    =  link_to(image_tag(@product.image_url(:large)), product_path(@product))
  .description
    = @product.full_description
    %p
    =render partial: 'price', locals: {product: @product}
