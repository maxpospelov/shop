= provide :title, @product.name
%h1= @product.name
.product
  .description
    = @product.full_description
    %form
      %table
        %tbody><
          %tr.variant
            %td
            %td
              %span.price= @product.price

      = button_to "в корзину"
